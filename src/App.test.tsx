import {describe,it,expect} from 'vitest';import {render,screen} from '@testing-library/react';import App from './App';describe('Minutia',()=>{it('renders the workspace',()=>{render(<App/>);expect(screen.getByText('MINUTIA')).toBeInTheDocument();expect(screen.getByText('Turn conversation into')).toBeInTheDocument();expect(screen.getByRole('button',{name:/Structure meeting/i})).toBeInTheDocument()})});